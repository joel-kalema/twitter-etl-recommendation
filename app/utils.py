# app/utils.py
def clean_text(text):
    # Placeholder function to clean text
    return text.lower().strip()

def score_tweet(tweet):
    # Placeholder function to score a tweet
    return len(tweet)
